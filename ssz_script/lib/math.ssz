lib consts = <consts.ssz>;
// Native core — PRNG (random/srand/rand/randI/randF + randseed state),
// trig (sin/cos/tan/asin/acos/atan/log/ln/exp/sqrt/ceil/floor/round), and
// classification (isfinite/isinf/isnan) — implemented in C++ at
// ssz_script/lib/math.cpp.  Only the template functions (min/max/...) stay
// in SSZ.
lib mn = <math>;

const double PI = 3.141592653589793238462643383279502884197;
const double E  = 2.718281828459045235360287471352662497757;

public double sin(double x)
{
  ret .mn.sin(x);
}

public double cos(double x)
{
  ret .mn.cos(x);
}

public double tan(double x)
{
  ret .mn.tan(x);
}

public double asin(double x)
{
  ret .mn.asin(x);
}

public double acos(double x)
{
  ret .mn.acos(x);
}

public double atan(double x)
{
  ret .mn.atan(x);
}

public double log(double x, double y)
{
  ret .mn.log(x, y);
}

public double ln(double x)
{
  ret .mn.ln(x);
}

public double exp(double x)
{
  ret .mn.exp(x);
}

public double sqrt(double x)
{
  ret .mn.sqrt(x);
}

public double ceil(double x)
{
  ret .mn.ceil(x);
}

public double floor(double x)
{
  ret .mn.floor(x);
}

public double round(double x)
{
  ret .mn.round(x);
}

public bool isfinite(double x)
{
  ret .mn.isfinite(x);
}

public bool isinf(double x)
{
  ret .mn.isinf(x);
}

public bool isnan(double x)
{
  ret .mn.isnan(x);
}

const int RANDMAX = .consts.int_t::MAX;

public int random()
{
  ret .mn.random();
}

public void srand(int s)
{
  .mn.srand(s);
}

public int rand(int min, int max)
{
  ret .mn.rand(min, max);
}

public int randI(int x, int y)
{
  ret .mn.randI(x, y);
}

public float randF(float x, float y)
{
  ret .mn.randF(x, y);
}

public ..min<_t>(_t x, _t y) _t
{
  ret x < y ? x : y;
}

public ..max<_t>(_t x, _t y) _t
{
  ret x > y ? x : y;
}

public bool inRange<_t>(_t start, _t end, _t x)
{
  ret start <= x && x <= end;
}

public void limMax<_t>(_t x=, _t y)
{
  x = .min!_t?(x, y);
}

public void limMin<_t>(_t x=, _t y)
{
  x = .max!_t?(x, y);
}

public void limRange<_t>(_t x=, _t low, _t high)
{
  x = .max!_t?(.min!_t?(x, high), low);
}

public void swap<_t>(_t x=, _t y=)
{
  *_t tmp = x;
  x = y;
  y = tmp;
}