public &OggVorbis
{
  index ptr = 0;
  new()
  {
    plugin index NewOggVorbis(::) = <ogg>;
    `ptr = NewOggVorbis(::);
  }
  delete()
  {
    plugin void DeleteOggVorbis(:index:) = <ogg>;
    DeleteOggVorbis(:`ptr:);
  }
  public bool open(^/char file)
  {
    plugin bool OggVorbisOpen(:index, ^/char:) = <ogg>;
    ret OggVorbisOpen(:`ptr, file:);
  }
  public void clear()
  {
    plugin void OggVorbisClear(:index:) = <ogg>;
    OggVorbisClear(:`ptr:);
  }
  public long pcmTotal()
  {
    plugin int OggVorbisPcmTotal(:index:) = <ogg>;
    ret OggVorbisPcmTotal(:`ptr:);
  }
  public int channels()
  {
    plugin int OggVorbisChannels(:index:) = <ogg>;
    ret OggVorbisChannels(:`ptr:);
  }
  public int rate()
  {
    plugin int OggVorbisRate(:index:) = <ogg>;
    ret OggVorbisRate(:`ptr:);
  }
  public index read(^short buffer)
  {
    plugin index OggVorbisRead(:index, ^short:) = <ogg>;
    ret OggVorbisRead(:`ptr, buffer:);
  }
  public int seek(double time)
  {
    plugin int OggVorbisSeek(:index, double:) = <ogg>;
    ret OggVorbisSeek(:`ptr, time:);
  }
}